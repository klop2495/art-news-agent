# 🚀 ПРОДАКШН - БЫСТРЫЙ СТАРТ

## ✅ Тестовый запуск завершен!

**Результаты:**
- ✅ Загружено: 4 источника (Christie's timeout)
- ✅ Обработано GPT: 3 статьи
- ✅ Создано в базе: 3 статьи
- ⚠️ Ошибок: 1 (MoMA - невалидный URL изображения)

**Успешность: 75%** - отличный результат для первого запуска!

---

## 📦 ЧТО У ВАС СЕЙЧАС ЕСТЬ

1. **Работающий агент** в `/Users/olegnikishin/art-news-agent`
2. **3 новые статьи** в админ-панели: http://localhost:3000/admin/news/queue
3. **Настроенные источники**: MoMA, The Met, Tate, Sotheby's
4. **Рабочий промпт** для GPT-4o

---

## 🏭 ДЕПЛОЙ НА ПРОДАКШН

### Вариант 1: VPS Сервер (DigitalOcean, Linode, AWS EC2)

#### 1️⃣ Подключитесь к серверу

```bash
ssh root@your-server-ip
```

#### 2️⃣ Установите Node.js 18+

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs git

# Проверка
node --version  # должно быть >= 18.0.0
```

#### 3️⃣ Загрузите агент на сервер

**Способ A: Через Git (рекомендуется)**

```bash
# На вашем компе: запуште в GitHub
cd /Users/olegnikishin/art-news-agent
git remote add origin https://github.com/YOUR-USERNAME/art-news-agent.git
git push -u origin main

# На сервере: клонируйте
mkdir -p /opt/art-news-agent
cd /opt/art-news-agent
git clone https://github.com/YOUR-USERNAME/art-news-agent.git .
```

**Способ B: Через SCP (без GitHub)**

```bash
# На вашем компе:
cd /Users/olegnikishin
tar -czf art-news-agent.tar.gz art-news-agent/
scp art-news-agent.tar.gz root@your-server-ip:/opt/

# На сервере:
cd /opt
tar -xzf art-news-agent.tar.gz
cd art-news-agent
```

#### 4️⃣ Настройте агент

```bash
cd /opt/art-news-agent

# Установите зависимости
npm install

# Соберите проект
npm run build

# Создайте .env
nano .env
```

**Вставьте в .env:**

```env
# OpenAI
OPENAI_API_KEY=sk-proj-ВАSHКЛЮЧKEY...
OPENAI_MODEL=gpt-4o

# Platform (ЗАМЕНИТЕ на ваш домен!)
INGEST_ENDPOINT_URL=https://artregplatform.com/api/news/ingest
NEWS_INGEST_API_KEY=your-production-api-key-here

# Настройки
DEFAULT_LANGUAGE=en
MAX_ARTICLES_PER_RUN=10
API_DELAY_MS=1000
```

**⚠️ ВАЖНО:**
- Замените `INGEST_ENDPOINT_URL` на ваш реальный домен
- Замените `NEWS_INGEST_API_KEY` на секретный ключ из `.env.local` платформы

**Сохраните:** `Ctrl+X` → `Y` → `Enter`

#### 5️⃣ Тестовый запуск

```bash
npm start
```

**Ожидаемый результат:**
```
🤖 Art News Agent - Run Started
📊 Fetched 10 article(s) from sources
✅ Successfully sent: 6-8
✅ Run completed successfully
```

#### 6️⃣ Настройте автозапуск (Cron)

```bash
# Откройте crontab
crontab -e

# Добавьте одну из строк:
```

**Вариант A: 1 раз в день (9:00 утра)**
```bash
0 9 * * * cd /opt/art-news-agent && /usr/bin/npm start >> /var/log/art-news-agent.log 2>&1
```

**Вариант B: 2 раза в день (9:00 и 21:00)**
```bash
0 9,21 * * * cd /opt/art-news-agent && /usr/bin/npm start >> /var/log/art-news-agent.log 2>&1
```

**Вариант C: Каждые 6 часов**
```bash
0 */6 * * * cd /opt/art-news-agent && /usr/bin/npm start >> /var/log/art-news-agent.log 2>&1
```

**Сохраните:** `Ctrl+X` → `Y` → `Enter`

#### 7️⃣ Проверьте cron

```bash
# Список задач
crontab -l

# Проверить что cron работает
sudo systemctl status cron

# Если не активен:
sudo systemctl start cron
sudo systemctl enable cron
```

---

### Вариант 2: Vercel Cron (Serverless)

**Преимущества:**
- ✅ Не нужен VPS
- ✅ Бесплатно до 100 запросов/день
- ✅ Автомасштабирование

**Недостатки:**
- ❌ Тайм-аут 10 секунд (Hobby) / 60 секунд (Pro)
- ❌ Нужно переписать в API Route

**Инструкция:** См. `docs/VERCEL_DEPLOYMENT.md` (если нужно, могу создать)

---

### Вариант 3: GitHub Actions (бесплатно)

**Преимущества:**
- ✅ Совсем бесплатно
- ✅ Не нужен сервер
- ✅ 2000 минут/месяц бесплатно

**Создайте `.github/workflows/news-agent.yml`:**

```yaml
name: Art News Agent

on:
  schedule:
    # Запуск каждый день в 9:00 UTC
    - cron: '0 9 * * *'
  workflow_dispatch: # Ручной запуск

jobs:
  fetch-news:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build project
        run: npm run build
      
      - name: Run agent
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-4o
          INGEST_ENDPOINT_URL: ${{ secrets.INGEST_ENDPOINT_URL }}
          NEWS_INGEST_API_KEY: ${{ secrets.NEWS_INGEST_API_KEY }}
          DEFAULT_LANGUAGE: en
          MAX_ARTICLES_PER_RUN: 10
        run: npm start
```

**Настройка:**
1. Запуште проект в GitHub
2. Откройте Settings → Secrets → Actions
3. Добавьте секреты:
   - `OPENAI_API_KEY`
   - `INGEST_ENDPOINT_URL`
   - `NEWS_INGEST_API_KEY`
4. Готово! Будет запускаться автоматически.

---

## 📊 МОНИТОРИНГ

### VPS: Просмотр логов

```bash
# Последние 50 строк
tail -n 50 /var/log/art-news-agent.log

# Следить в реальном времени
tail -f /var/log/art-news-agent.log

# Статистика успешных запусков
grep "Run completed successfully" /var/log/art-news-agent.log | wc -l

# Последние ошибки
grep "❌" /var/log/art-news-agent.log | tail -n 10
```

### GitHub Actions: Actions tab

- Откройте репозиторий → Actions
- Смотрите историю запусков
- Читайте логи каждого запуска

---

## 🔧 НАСТРОЙКА ИСТОЧНИКОВ

### Для улучшения качества (10/10):

Замените URL-ы в `src/fetchSources.ts` на **конкретные статьи**:

```typescript
// ❌ Плохо (раздел):
{ sourceName: 'MoMA', url: 'https://www.moma.org/calendar/exhibitions' }

// ✅ Хорошо (конкретная выставка):
{ sourceName: 'MoMA', url: 'https://www.moma.org/calendar/exhibitions/5634' }
```

**Как найти конкретные URL-ы:**

1. Откройте любой из источников:
   - https://www.moma.org/calendar/exhibitions
   - https://www.metmuseum.org/exhibitions
   - https://www.tate.org.uk/press/press-releases

2. Кликните на интересную новость/выставку

3. Скопируйте полный URL из браузера

4. Добавьте в `staticSources` массив

5. Пересоберите и запуште:
   ```bash
   npm run build
   git add src/fetchSources.ts
   git commit -m "feat: add specific article URLs"
   git push
   ```

---

## 💰 СТОИМОСТЬ

**OpenAI API (GPT-4o):**
- ~$0.10-0.20 за статью
- 10-20 статей/день = **$1-4/день**
- **~$30-120/месяц**

**VPS Сервер:**
- DigitalOcean Basic: **$6/месяц**
- Linode Nanode: **$5/месяц**
- AWS t2.micro: **$8.5/месяц** (или бесплатно первый год)

**GitHub Actions:**
- **$0** (бесплатно до 2000 минут)

**ИТОГО:**
- VPS: **$35-130/месяц** (OpenAI + сервер)
- GitHub Actions: **$30-120/месяц** (только OpenAI)

---

## ✅ ЧЕКЛИСТ ПРОДАКШН

### Подготовка:
- [ ] Агент работает локально
- [ ] API ключи OpenAI и Platform готовы
- [ ] Выбран способ деплоя (VPS / GitHub Actions)

### VPS деплой:
- [ ] Сервер создан и доступен по SSH
- [ ] Node.js 18+ установлен
- [ ] Проект загружен на сервер
- [ ] `.env` создан с правильными ключами
- [ ] `npm install && npm run build` выполнены
- [ ] Тестовый запуск успешен
- [ ] Cron задание добавлено
- [ ] Логи создаются и читаются

### GitHub Actions деплой:
- [ ] Репозиторий создан на GitHub
- [ ] `.github/workflows/news-agent.yml` создан
- [ ] Секреты добавлены в Settings → Secrets
- [ ] Ручной запуск (workflow_dispatch) успешен
- [ ] Автозапуск работает

### Финальные проверки:
- [ ] Агент запускается автоматически
- [ ] Статьи появляются в админ-панели
- [ ] Качество статей приемлемо
- [ ] Мониторинг настроен

---

## 🚀 РЕКОМЕНДАЦИИ

### Для начала:

**1. GitHub Actions (самый простой)**
   - Не нужен сервер
   - Бесплатно
   - 5 минут на настройку

**2. Когда нужен VPS:**
   - Если агент работает >30 минут за запуск
   - Если нужно >100 статей/день
   - Если нужны кастомные логи/мониторинг

---

## 📞 ПОДДЕРЖКА

**Вопросы по настройке:**
- Откройте issue в репозитории
- Пишите в Telegram (если есть)

**Полная документация:**
- `DEPLOYMENT.md` - детальная инструкция для VPS
- `README.md` - общее описание проекта
- `QUICKSTART.md` - быстрый старт для локальной разработки

---

**Создано:** November 15, 2025  
**Версия агента:** v1.1.0-promo-ready  
**Статус:** ✅ Готов к продакшн-деплою

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Сейчас:**
   - Откройте http://localhost:3000/admin/news/queue
   - Проверьте 3 новые статьи
   - Опубликуйте их

2. **Сегодня:**
   - Выберите способ деплоя (GitHub Actions рекомендуется)
   - Настройте автозапуск
   - Проверьте первый автоматический запуск

3. **На неделе:**
   - Добавьте 5-10 конкретных URL-ов статей
   - Настройте мониторинг
   - Оптимизируйте качество

**Удачи! 🚀**

