# 🚀 GitHub Actions - Настройка за 5 минут

## Преимущества

- ✅ **Бесплатно** (2000 минут/месяц)
- ✅ **Не нужен VPS** сервер
- ✅ **Автозапуск** каждый день в 9:00 UTC
- ✅ **Ручной запуск** через GitHub UI
- ✅ **Логи** всех запусков
- ✅ **5 минут** на настройку

---

## 📋 Шаги настройки

### 1️⃣ Создайте репозиторий на GitHub

```bash
# На вашем компе
cd /Users/olegnikishin/art-news-agent

# Инициализируйте git (если еще не сделали)
git init

# Добавьте все файлы
git add .
git commit -m "Initial commit: Art News Agent"

# Создайте репозиторий на GitHub.com
# Затем подключите его:
git remote add origin https://github.com/YOUR-USERNAME/art-news-agent.git
git branch -M main
git push -u origin main
```

**Или используйте GitHub Desktop / GitHub CLI.**

---

### 2️⃣ Добавьте секреты

1. Откройте ваш репозиторий на GitHub.com
2. Перейдите: **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте 3 секрета:

#### Секрет 1: `OPENAI_API_KEY`

**Name:** `OPENAI_API_KEY`  
**Value:** 
```
sk-proj-ВАSHКЛЮЧKEY...
```
*(Используйте ваш реальный OpenAI API ключ)*

---

#### Секрет 2: `INGEST_ENDPOINT_URL`

**Name:** `INGEST_ENDPOINT_URL`  
**Value:** 
```
https://artregplatform.com/api/news/ingest
```

⚠️ **Замените на ваш реальный домен!**

Для локального тестирования (не рекомендуется для продакшн):
```
http://localhost:3000/api/news/ingest
```

Для продакшн:
```
https://your-actual-domain.com/api/news/ingest
```

---

#### Секрет 3: `NEWS_INGEST_API_KEY`

**Name:** `NEWS_INGEST_API_KEY`  
**Value:** (скопируйте из `.env.local` вашей платформы)

```
your-production-api-key-here
```

**Где найти:**
1. Откройте `/Users/olegnikishin/art-registry-platform/.env.local`
2. Найдите строку `NEWS_INGEST_API_KEY=...`
3. Скопируйте значение

---

### 3️⃣ Проверьте workflow

1. Откройте ваш репозиторий на GitHub
2. Перейдите: **Actions** tab
3. Вы увидите workflow "Art News Agent"

---

### 4️⃣ Ручной запуск (тест)

1. В **Actions** → **Art News Agent**
2. Нажмите **Run workflow** (справа)
3. (Опционально) Измените "Max articles per run" на `3` для теста
4. Нажмите **Run workflow** (зеленая кнопка)

**Ожидаемый результат:**

```
🚀 Starting Art News Agent...
🤖 Art News Agent - Run Started
📊 Fetched 3 article(s) from sources
✅ Successfully sent: 2-3
✅ Run completed successfully
✅ Agent run completed
```

**Время:** ~2-3 минуты

---

### 5️⃣ Настройте расписание (уже сделано!)

Workflow уже настроен на автозапуск **каждый день в 9:00 UTC**.

**Изменить расписание:**

Отредактируйте `.github/workflows/news-agent.yml`:

```yaml
on:
  schedule:
    # ┌───────────── минута (0 - 59)
    # │ ┌───────────── час (0 - 23)
    # │ │ ┌───────────── день месяца (1 - 31)
    # │ │ │ ┌───────────── месяц (1 - 12)
    # │ │ │ │ ┌───────────── день недели (0 - 6)
    # │ │ │ │ │
    # * * * * *
    
    # Каждый день в 9:00 UTC (12:00 MSK)
    - cron: '0 9 * * *'
    
    # Два раза в день: 9:00 и 21:00 UTC
    # - cron: '0 9,21 * * *'
    
    # Каждые 6 часов
    # - cron: '0 */6 * * *'
```

**Commit и push:**

```bash
git add .github/workflows/news-agent.yml
git commit -m "Update cron schedule"
git push
```

---

## 📊 Мониторинг

### Просмотр логов

1. Откройте **Actions** tab
2. Кликните на любой запуск
3. Кликните на "Fetch and Process News"
4. Разверните "🤖 Run News Agent"

**Что смотреть:**

- ✅ "Run completed successfully" - успех
- ❌ "Error" - проблемы
- 📊 "Successfully Sent: X" - количество статей

---

### История запусков

- **Actions** tab показывает все запуски за последние 90 дней
- Зеленая галочка ✅ - успех
- Красный крестик ❌ - ошибка
- Желтый кружок 🟡 - в процессе

---

## 🔧 Настройка источников

### Улучшить качество статей

Замените URL-ы в `src/fetchSources.ts` на конкретные статьи:

```typescript
// ❌ Плохо:
{ sourceName: 'MoMA', url: 'https://www.moma.org/calendar/exhibitions' }

// ✅ Хорошо:
{ sourceName: 'MoMA', url: 'https://www.moma.org/calendar/exhibitions/5634' }
```

**После изменений:**

```bash
npm run build
git add src/fetchSources.ts dist/
git commit -m "feat: add specific article URLs"
git push
```

**Автоматически применится при следующем запуске!**

---

## 💰 Стоимость

**GitHub Actions:**
- ✅ Бесплатно до **2000 минут/месяц** (Public repos)
- ✅ Бесплатно до **500 МБ** artifact storage

**Один запуск агента:**
- ~2-5 минут за запуск
- ~60-150 минут/месяц (при 1 запуске/день)
- ~120-300 минут/месяц (при 2 запусках/день)

**Итого:** **БЕСПЛАТНО** 🎉

**OpenAI API:**
- ~$30-120/месяц (зависит от количества статей)

---

## ⚙️ Опции запуска

### Ручной запуск с параметрами

1. **Actions** → **Art News Agent** → **Run workflow**
2. Укажите "Max articles per run":
   - `1` - быстрый тест (30 сек)
   - `3` - короткий тест (2 мин)
   - `10` - стандартный (5 мин)
   - `20` - полный (10 мин)

---

## 🚨 Troubleshooting

### ❌ Workflow не запускается автоматически

**Причина:** Новые репозитории требуют первого ручного запуска.

**Решение:**
1. Сделайте ручной запуск (Run workflow)
2. После этого cron будет работать автоматически

---

### ❌ Ошибка "401 Unauthorized"

**Причина:** Неверный API ключ.

**Решение:**
1. Проверьте секреты в **Settings** → **Secrets**
2. Пересоздайте `OPENAI_API_KEY` или `NEWS_INGEST_API_KEY`

---

### ❌ Ошибка "Connection refused"

**Причина:** Неверный `INGEST_ENDPOINT_URL` (возможно, localhost).

**Решение:**
1. Убедитесь что `INGEST_ENDPOINT_URL` указывает на **публичный домен**
2. Не используйте `localhost` в GitHub Actions
3. Деплойте платформу на Vercel/Railway/DigitalOcean
4. Обновите секрет `INGEST_ENDPOINT_URL`

---

### ❌ Агент обрабатывает те же статьи повторно

**Причина:** `processed-articles.json` не сохраняется между запусками.

**Решение:** Это нормально для GitHub Actions (stateless).

**Опция 1:** Используйте GitHub Artifacts (сложно)  
**Опция 2:** Полагайтесь на `external_id` дедупликацию в платформе  
**Опция 3:** Используйте VPS для stateful хранилища

---

## ✅ Чеклист

- [ ] Репозиторий создан на GitHub
- [ ] Файл `.github/workflows/news-agent.yml` запушен
- [ ] 3 секрета добавлены (OPENAI_API_KEY, INGEST_ENDPOINT_URL, NEWS_INGEST_API_KEY)
- [ ] Ручной запуск успешен
- [ ] Статьи появились в админ-панели
- [ ] Расписание настроено (по умолчанию 9:00 UTC)

---

## 🎯 Рекомендации

### Для начала:

1. **Запустите вручную 1 раз** для теста
2. **Проверьте статьи** в админ-панели
3. **Дайте автозапуску** работать 2-3 дня
4. **Добавьте конкретные URL-ы** для лучшего качества

### Если нужно больше:

- **>100 статей/день** → Используйте VPS
- **Статьи >10 минут обработки** → Используйте VPS
- **Нужны stateful логи** → Используйте VPS

**Но для 10-20 статей/день GitHub Actions ИДЕАЛЕН! 🚀**

---

## 📞 Поддержка

**Проблемы:**
- Откройте Issue в репозитории
- Проверьте логи в Actions tab

**Документация:**
- `PRODUCTION_QUICKSTART.md` - полное руководство
- `DEPLOYMENT.md` - VPS инструкции
- `README.md` - общее описание

---

**Создано:** November 15, 2025  
**Версия:** v1.1.0-promo-ready  
**Статус:** ✅ Готов к GitHub Actions деплою

**ЛЕГКО! БЕСПЛАТНО! РАБОТАЕТ! 🎉**

