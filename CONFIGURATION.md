# 🔧 Конфигурация News Agent

## 🚨 ВАЖНО: Проблема с главными страницами

**❌ НЕ РАБОТАЕТ:**
```typescript
{ sourceName: 'The Art Newspaper', url: 'https://www.theartnewspaper.com' }
```
Причина: GPT получает главную страницу с заголовками, а не полные статьи.

**✅ РАБОТАЕТ:**
```typescript
{ sourceName: 'The Art Newspaper', url: 'https://www.theartnewspaper.com/2024/11/15/конкретная-статья' }
```
Причина: GPT получает полный текст конкретной статьи.

---

## 📋 Как добавлять источники новостей

### **Метод 1: Конкретные статьи** (для разовых запусков)

1. Откройте новостной сайт (например, Artnet News)
2. Найдите интересную статью
3. Скопируйте полный URL статьи
4. Добавьте в `src/fetchSources.ts`:

```typescript
const staticSources = [
  {
    sourceName: 'Artnet News',
    url: 'https://news.artnet.com/art-world/moma-new-exhibition-2024',
  },
  {
    sourceName: 'Hyperallergic',
    url: 'https://hyperallergic.com/876543/artist-interview-2024',
  },
  // ... добавьте 10-20 статей
];
```

**Преимущества:**
- ✅ Гарантированно полный текст
- ✅ Вы контролируете какие статьи обрабатываются
- ✅ Высокое качество контента

**Недостатки:**
- ❌ Нужно вручную обновлять URL-ы
- ❌ Не подходит для автоматизации (cron)

---

### **Метод 2: RSS Feeds** (РЕКОМЕНДУЕТСЯ для автоматизации)

**Что такое RSS?**
RSS feed - это список последних статей сайта с их URL-ами.

**Как найти RSS feed сайта:**
1. Обычно находится по адресу `/feed`, `/rss`, или `/rss.xml`
2. Примеры:
   - `https://hyperallergic.com/feed/`
   - `https://www.theartnewspaper.com/rss`
   - `https://news.artnet.com/feed`

**Как использовать RSS (требует доработки кода):**

Нужно добавить RSS парсер. Пример концепции:

```typescript
// src/rssParser.ts (новый файл)
import Parser from 'rss-parser';

export async function fetchFromRSS(feedUrl: string, limit = 5) {
  const parser = new Parser();
  const feed = await parser.parseURL(feedUrl);
  
  return feed.items.slice(0, limit).map(item => ({
    sourceName: feed.title || 'Unknown',
    url: item.link || '',
  }));
}
```

**Установить зависимость:**
```bash
npm install rss-parser
npm install --save-dev @types/rss-parser
```

---

### **Метод 3: Web Scraping** (сложный, но мощный)

Парсить главные страницы, извлекать ссылки на статьи, затем загружать их.

**Пример:**
```typescript
async function scrapeArticleLinks(url: string): Promise<string[]> {
  const html = await fetch(url).then(r => r.text());
  const $ = cheerio.load(html);
  
  const links: string[] = [];
  $('article a').each((i, el) => {
    const href = $(el).attr('href');
    if (href && href.includes('/art-world/') || href.includes('/news/')) {
      links.push(href);
    }
  });
  
  return links.slice(0, 5); // First 5 articles
}
```

---

## 🎯 РЕКОМЕНДУЕМАЯ СТРАТЕГИЯ

### **Для начала (сейчас):**

**Вручную добавьте 10-20 качественных статей:**

1. Откройте эти сайты:
   - https://news.artnet.com
   - https://hyperallergic.com
   - https://www.theartnewspaper.com
   - https://www.artnews.com
   - https://www.artforum.com

2. Найдите свежие (1-2 дня) статьи о:
   - Крупных выставках
   - Аукционах
   - Новостях музеев
   - Арт-рынке

3. Добавьте URL-ы в `src/fetchSources.ts`:

```typescript
const staticSources = [
  // Выставки
  {
    sourceName: 'Artnet News',
    url: 'https://news.artnet.com/exhibitions/moma-2024-exhibition',
  },
  {
    sourceName: 'The Art Newspaper',
    url: 'https://www.theartnewspaper.com/2024/11/tate-modern-opens',
  },
  
  // Аукционы
  {
    sourceName: 'Artnet News',
    url: 'https://news.artnet.com/market/christies-auction-results',
  },
  
  // Музеи
  {
    sourceName: 'Hyperallergic',
    url: 'https://hyperallergic.com/876543/met-museum-acquisition',
  },
  
  // ... добавьте еще 10-15 статей
];
```

4. Запустите агент:
```bash
cd art-news-agent
npm run build
npm start
```

5. Проверьте результаты в админ-панели:
```
http://localhost:3000/admin/news/queue
```

---

### **Для автоматизации (позже):**

**Вариант A: RSS Feeds**
- Добавить `rss-parser`
- Создать функцию `fetchFromRSS()`
- Заменить `staticSources` на динамическую загрузку из RSS
- Запускать через cron 1-2 раза в день

**Вариант B: Парсинг сайтов**
- Создать функцию scraping для каждого источника
- Извлекать ссылки на свежие статьи
- Обрабатывать их через GPT

**Вариант C: NewsAPI / другие агрегаторы**
- Использовать платные API типа NewsAPI.org
- Фильтровать по ключевым словам: "art", "museum", "exhibition"
- Обрабатывать через GPT

---

## 🔍 Проверка качества

После добавления источников проверьте:

1. **Content length** (должно быть >1000 символов):
```
[GPT] Content: 2543 chars ✅  (хорошо)
[GPT] Content: 203 chars  ❌  (слишком мало - проблема!)
```

2. **Логи агента** покажут проблемы:
```bash
tail -f agent.log
```

3. **Админ-панель** - прочитайте несколько статей:
```
http://localhost:3000/admin/news/queue
```

Если content = excerpt → статья парсилась неправильно (главная страница вместо статьи).

---

## 📝 Текущая конфигурация

**Файл:** `src/fetchSources.ts`

**Текущие источники:** ПУСТЫЕ (примеры-заглушки)

**Действия:**
1. Откройте `src/fetchSources.ts`
2. Замените примеры на реальные URL-ы статей
3. Пересоберите: `npm run build`
4. Запустите: `npm start`

---

## 🚀 Быстрый старт (прямо сейчас)

```bash
# 1. Откройте файл конфигурации
code src/fetchSources.ts

# 2. Найдите 10 статей с новостных сайтов

# 3. Добавьте их URL-ы в staticSources

# 4. Пересоберите
npm run build

# 5. Запустите
npm start

# 6. Проверьте результат
# Откройте http://localhost:3000/admin/news/queue
```

---

## 🗓️ Фильтрация по датам (важно)

- Агент теперь извлекает `publish_date` и `event_dates` (start/end, timezone) из пресс-релизов.
- Статьи автоматически пропускаются, если дата публикации **прошла** и нет будущих событий (ярмарка/выставка уже завершилась).
- В логах появится `⚠️  Skipping outdated material...` — это означает, что новость неактуальна.
- Чтобы материал прошёл фильтр:
  - выбирайте страницы с анонсами на сегодня/будущее (новые выставки, грядущие ярмарки, свежие аукционы);
  - проверяйте, что даты в тексте не старше текущего дня;
  - для мероприятий без даты оставляйте только самые свежие релизы (агент подставит сегодняшнюю дату).
- Если релиз сообщает о прошедшем событии, агент отметит его как `event_dates.status = "past"` и не отправит в платформу.

---

## ❓ FAQ

**Q: Сколько статей добавлять?**  
A: Для теста - 5-10. Для production - 20-30 качественных источников.

**Q: Как часто обновлять?**  
A: Вручную - раз в неделю. С RSS - автоматически раз в день.

**Q: Можно ли миксовать разные сайты?**  
A: Да! Рекомендуется 3-5 разных источников для разнообразия.

**Q: Что делать с paywall-статьями?**  
A: Использовать только open-access источники (Hyperallergic, Artnet и т.д.)

---

## 📞 Нужна помощь?

1. Проверьте логи: `tail -f agent.log`
2. Проверьте длину content в логах
3. Откройте issue в репозитории

---

**Последнее обновление:** November 15, 2025

