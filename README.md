# Art News Agent 🤖

**External GPT-powered news aggregation agent for Art Registry Platform**

Автоматический агент для сбора, обработки и публикации новостей из мира искусства с использованием GPT-5.1.

---

## 🎯 Функции

- ✅ Сбор статей из источников новостей об искусстве
- ✅ Обработка через GPT-5.1 Instant (извлечение структурированных данных)
- ✅ Валидация качества контента и проверка фактов
- ✅ Отправка на платформу Art Registry через API
- ✅ Дедупликация (предотвращение повторных публикаций)
- ✅ Rate limiting и retry логика
- ✅ Детальное логирование и статистика

---

## 📋 Требования

- **Node.js** >= 18.0.0
- **OpenAI API key** (GPT-5.1 доступ)
- **Art Registry Platform API key**

---

## 🚀 Установка

```bash
# Клонировать репозиторий
cd art-news-agent

# Установить зависимости
npm install

# Настроить окружение
cp .env.example .env
# Отредактировать .env (добавить ключи)
```

---

## ⚙️ Конфигурация

Отредактируйте `.env`:

```env
# OpenAI API Key
OPENAI_API_KEY=sk-proj-...

# Модель (gpt-5.1-instant рекомендуется)
OPENAI_MODEL=gpt-5.1-instant

# Endpoint платформы
INGEST_ENDPOINT_URL=https://artregplatform.com/api/news/ingest

# API Key платформы
NEWS_INGEST_API_KEY=your-secret-key

# Язык контента
DEFAULT_LANGUAGE=ru

# Макс статей за запуск (10-20 рекомендуется)
MAX_ARTICLES_PER_RUN=20

# Задержка между запросами (мс)
API_DELAY_MS=1000
```

---

## 🎮 Использование

### Development (разработка)
```bash
npm run dev
```

### Production (прод)
```bash
# Собрать
npm run build

# Запустить
npm start
```

### Проверка доступных моделей
```bash
npm run check-models
```

---

## ⏰ Настройка Cron (автозапуск)

### Linux/macOS

```bash
# Открыть crontab
crontab -e

# Добавить строку (запуск каждый день в 9:00)
0 9 * * * cd /path/to/art-news-agent && npm start >> agent.log 2>&1

# Или каждые 6 часов
0 */6 * * * cd /path/to/art-news-agent && npm start >> agent.log 2>&1

# Или 2 раза в день (9:00 и 21:00)
0 9,21 * * * cd /path/to/art-news-agent && npm start >> agent.log 2>&1
```

### Примеры расписаний

```bash
# Каждый час
0 * * * * cd /path/to/art-news-agent && npm start >> agent.log 2>&1

# Каждый день в 9:00
0 9 * * * cd /path/to/art-news-agent && npm start >> agent.log 2>&1

# Каждый будний день в 10:00 и 18:00
0 10,18 * * 1-5 cd /path/to/art-news-agent && npm start >> agent.log 2>&1
```

---

## 📁 Структура проекта

```
art-news-agent/
├── src/
│   ├── types.ts          # TypeScript интерфейсы
│   ├── gptClient.ts      # GPT-5.1 клиент
│   ├── ingestClient.ts   # API клиент платформы
│   ├── fetchSources.ts   # Парсинг источников
│   └── index.ts          # Главный файл
├── dist/                 # Скомпилированные файлы
├── package.json
├── tsconfig.json
├── .env                  # Конфигурация (не в git)
├── .env.example          # Пример конфигурации
├── processed-articles.json  # БД обработанных статей
└── README.md
```

---

## 🔧 Добавление источников

Отредактируйте `src/fetchSources.ts`:

```typescript
const staticSources = [
  { 
    sourceName: 'The Art Newspaper', 
    url: 'https://www.theartnewspaper.com/...' 
  },
  { 
    sourceName: 'Artnet News', 
    url: 'https://news.artnet.com/...' 
  },
  // Добавьте свои источники
];

// Добавьте селекторы для домена
const DOMAIN_SELECTORS = {
  'example.com': '.article-content',
};
```

---

## 🏗 Архитектура

```
┌─────────────────┐
│  News Sources   │ (RSS, HTML, APIs)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ fetchSources.ts │ (Cheerio parser)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  gptClient.ts   │ (GPT-5.1 extracts)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ ingestClient.ts │ (POST to platform)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│  Art Registry   │ (Editorial review)
│    Platform     │ → Publish
└─────────────────┘
```

---

## 💰 Стоимость

**GPT-5.1 Instant pricing** (примерно):
- Input: ~$3-5 / 1M tokens
- Output: ~$12-15 / 1M tokens

**Пример расчёта:**
- 10-20 статей/день
- ~5000 токенов вход + 2000 токенов выход на статью
- **~$1-2/день = $30-60/месяц**

---

## 🐛 Troubleshooting

### "Empty response from OpenAI"
→ Проверьте `OPENAI_API_KEY` в `.env`

### "Model gpt-5.1-instant not available"
→ GPT-5.1 еще не в API, используйте `gpt-4o`:
```env
OPENAI_MODEL=gpt-4o
```

### "Ingest failed with status 401"
→ Проверьте `NEWS_INGEST_API_KEY` - должен совпадать с платформой

### "No articles to process"
→ Добавьте источники в `src/fetchSources.ts`

### "Rate limited"
→ Увеличьте `API_DELAY_MS` в `.env`:
```env
API_DELAY_MS=2000
```

---

## 📊 Логи

Просмотр логов:
```bash
# Последние 50 строк
tail -n 50 agent.log

# Следить в реальном времени
tail -f agent.log

# Поиск ошибок
grep "Error" agent.log
```

---

## 🔐 Безопасность

- ❌ **НЕ** коммитьте `.env` в git
- ✅ Используйте надежные API ключи
- ✅ Регулярно ротируйте ключи
- ✅ Ограничьте доступ к серверу

---

## 📝 Лицензия

MIT

---

## 👨‍💻 Поддержка

Вопросы и issues: создайте issue в репозитории

**Art Registry Platform** © 2025

