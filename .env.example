# OpenAI API Key
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# OpenAI Model (gpt-5.1-instant is recommended for news processing)
# Available: gpt-5.1-instant, gpt-5.1-thinking, gpt-4o (legacy)
OPENAI_MODEL=gpt-5.1-instant

# Art Registry Platform Ingest Endpoint
# Production: https://artregplatform.com/api/news/ingest
# Local dev: http://localhost:3000/api/news/ingest
INGEST_ENDPOINT_URL=http://localhost:3000/api/news/ingest

# API Key for News Ingest (must match NEWS_INGEST_API_KEY in platform .env.local)
NEWS_INGEST_API_KEY=your-super-secret-key-here

# Default language for news content (ru, en, etc.)
DEFAULT_LANGUAGE=ru

# Max articles to process per run (10-20 recommended for daily runs)
MAX_ARTICLES_PER_RUN=20

# Delay between API calls in milliseconds (avoid rate limiting)
API_DELAY_MS=1000
